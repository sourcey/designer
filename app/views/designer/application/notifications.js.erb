<% if flash.any? %>
  toastr.clear()
<% end %>

<% flash.each do |type, message| %>
  <% if type == 'error' %>
    toastr.error("<%= message %>")
  <% elsif type == 'notice' %>
    toastr.success("<%= message %>")
  <% else %>
    toastr.info("<%= message %>")
  <% end %>
<% end %>
